{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "command": "python -c 'import sys; sys.path.insert(0, \".\"); exec(open(\"teaching_assistant/main.py\").read())'"
  }
}