{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "command": "PYTHONPATH=.:$PYTHONPATH python teaching_assistant/main.py"
  }
}